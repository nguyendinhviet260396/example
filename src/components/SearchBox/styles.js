import { blue } from "@material-ui/core/colors";

const styles=()=>({
    inputSearch:{
        width: '30%',
        color: blue,
        fontSize:20,
    },
    container:{}
});
export default styles;
    